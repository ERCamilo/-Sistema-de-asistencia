# F1.4 — Empleados, puestos y líderes por proyecto

| Campo | Valor |
|---|---|
| Orden | Dirección (2026-08-25): F1.4 autorizada exclusivamente; F1.5 asistencia BLOQUEADA |
| Fecha | 2026-08-25 |
| Estado | ✅ COMPLETADO (2 tandas) — detenido esperando autorización F1.5 |
| Suite final | **350/350 suites · 3358 tests · 0 fallos** |

## Criterios obligatorios de Dirección

| Prueba exigida | Resultado |
|---|---|
| A tiene Juan y B tiene Pedro → cambiar A↔B nunca mezcla listas | ✅ 3 ciclos de switch con recarga completa: empleados, líderes y posiciones siempre aislados |
| A y B pueden usar el número #12 simultáneamente | ✅ Identidad interna = employeeId (+projectId como contenedor); dedup con clave compuesta `effectiveProjectId::number`; cero conflictos cruzados |
| Ninguna referencia puesto↔líder cruza proyectos | ✅ Guard pairwise: binding B→líder-de-A se anula vía path existente de huérfanos; mismo-proyecto sobrevive revalidación |

## Tanda 1 — Núcleo project-aware (~183 líneas impl. + 408 tests)

| Aspecto | Decisión |
|---|---|
| Campo `projectId` en las 3 clases | Patrón hasOwnProperty (espejo exacto del fix H-01/deletedAt): ausencia se preserva como ausencia — registros legacy byte-estables en TODOS los puntos de inflación |
| Resolución de alcance | `getEntityScope()` → `{enabled, projectId, defaultProjectId}` + `peekEntityScope()` sincrónico para renders/handlers; snapshots preparados por boot y por cambio de proyecto |
| Nacimiento con sello | Los 3 puntos reales son los modals (`EmployeeModal.applyFields` — el binding de EmployeesUI.saveEmployee estaba muerto—, LeaderModal.save, PositionModal.save); spread condicional ⇒ flag OFF no escribe la clave |
| Filtrado de vistas | Gate compartido para empleados+líderes, grilla de posiciones, pickers multi-filtro y picker de líder dentro de PositionModal; fail-open con flag OFF |
| Merge/sync | El projectId PROPIO de cada entidad viaja intacto desde payload/store — jamás se sobreescribe con el proyecto activo del momento |
| Dedup/números | Clave compuesta `effectiveProjectId::number` en analyzeConflicts y generación de sugeridos; flag OFF ⇒ número crudo (comportamiento idéntico al actual) |
| Referencial | Extensión de la condición de huérfanos en PersistenceService: líder inexistente O de otro proyecto efectivo ⇒ null (pairwise, independiente del activo) |

**Regla de oro respetada:** flag OFF ⇒ cero cambios de datos/conducta (controles OFF de paridad incluidos en tests).

## Tanda 2 — Migración M2 + batería end-to-end (~144 impl. + 352 tests)

- **Backup ANTES de escribir** (regla #9): snapshot cloud `pre-migration` reutilizando la API existente si hay sesión; offline/fallo ⇒ `backup:'unavailable'` + warn + proceder (justificado: sellado puramente aditivo y reversible por flag — F0.4 §6).
- **Job idempotente e interrumpible**: chunks de 50 con yield; marcador LS `migration.projectStamp.v1` por-store; resume probado matando a mitad y re-corriendo sin duplicados ni pérdidas.
- **Hook de arranque**: ProjectsBoot dispara fire-and-forget con catch→warn solo con singletons reales y flag ON; contrato never-throw intacto.
- Batería S1–S6: legacy→predeterminado ✔ · coexistencia #12 ✔ · tombstone cross-safe con `deletedAt`+`projectId` sobreviviendo juntos (H-01 blindado post-projectId) ✔ · merge entrante conserva proyecto propio aunque el activo sea otro ✔ · resume ✔ · flag OFF no-op ✔.

## Desvíos declarados

| Presupuesto | Asignado | Real | Justificación |
|---|---|---|---|
| Tanda 1 | ≤520 | ~591 | Matriz de pruebas exigida por Dirección (switches ×3 tipos de lista, dedup modal-level, guards pairwire, controles OFF) no comprime sin droppear un criterio; implementación quedó en 183 |
| Tanda 2 | ≤450 | ~516 | Harness fresh-module-per-phase + bucles de recarga son lo que hace la batería genuinamente end-to-end |

## Micro-slice de cierre (obligatorio según Dirección) — ✅ RESUELTO

La Dirección reclasificó el dedup global de puestos como problema conceptual (no cosmético). Corregido:

| Sitio | Antes | Después |
|---|---|---|
| `PositionModal.js:338` (guard de guardado) | slug GLOBAL: bloqueaba crear Y **editar** un puesto si otra obra tenía homónimo | comparación pairwise vía `sameEffectiveProject` (= `effectiveProjectId::nameSlug`, patrón espejo del número de empleado, con `peekEntityScope()`) |
| `PersistenceService.sanitizePositions` (~:1705) | merge global `masterIdBySlug`: absorbía el puesto de B dentro del de A | clave compuesta `effectiveProjectId::slug` cuando scope activo; slug crudo en legacy |

Tests RED→GREEN (5 nuevos): creación independiente A/B ✔ · edición de A sin tocar B (antes BLOQUEADA) ✔ · borrado independiente ✔ · duplicado real mismo-proyecto conserva manejo legado ✔ · sanitize no absorbe cross-project ✔ · control flag OFF byte-parity ✔.
**Suite: 350/350 suites · 3363 tests · 0 fallos.**

## Limitaciones conocidas restantes (deuda de UI aceptada por Dirección)

1. Contadores de encabezado de listas (`state.employees.length`) siguen sin escalar por proyecto — visible solo con flag ON y >1 proyecto.
2. Píldoras de filtro del fallback legado permanecen sin scope.
→ Deuda de UI acordada; no afecta integridad de datos.

## Checklist de la orden

- [x] Entidades realmente project-aware (no solo el campo): nacimiento/lectura/escritura/merge/borrado/sync/validaciones
- [x] M2 local idempotente con backup previo e interrupción/reanudación
- [x] Embebidos (préstamos/adelantos/bonos/deducciones/fotos/historial) siguen al empleado — sin migración independiente
- [x] `Position.leaderId` mismo-proyecto únicamente
- [x] Sin clonación (F2.7) — modelo listo para futuro `copiedFromEmployeeId`
- [x] Batería A/B completa + H-01 protegido post-projectId
- [x] Asistencia/nómina/caja SIN TOCAR
- [x] DETENERSE — F1.5 requiere autorización explícita
