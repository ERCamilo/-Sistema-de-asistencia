# F1.3 — ProjectContext / activeProjectId

| Campo | Valor |
|---|---|
| Paso | Fase 1 / F1.3 — hito `project-context` |
| Fecha | 2026-08-25 |
| Estado | ✅ Ejecutado + segunda vuelta: PASS con nota menor |
| Criterio roadmap | Toda operación puede obtener proyecto activo ✔ (accessor listo; consumidores llegan en F1.4+, por diseño) |

## Qué se construyó

`js/modules/features/projects/ProjectContext.js` (87 líneas):

- LS key `asistencia_active_project_id` (preferencia device-local; NO va al manifiesto de wipe — misma familia que los flags; comentado en el módulo).
- `getActiveProjectId()`: id almacenado válido+y ACTIVO → si no, default (vía ensureDefaultProject) → si no, null. **Flag OFF ⇒ siempre null** (modo legado).
- `setActiveProjectId(id)`: valida existencia + status activo ANTES de persistir; rechazo async con Error descriptivo (español, copia del repo), sin escribir nada inválido.
- `clearActiveProjectId()`.
- Recovery: valor LS apuntando a proyecto borrado/inexistente ⇒ fallback limpio al default, jamás explota hacia el caller.
- Toggle de flag a mitad de sesión: seguro — cada entrada relee el flag, nada cacheado (verificado en revisión).

## Segunda vuelta

- Cadena de fallback y validaciones: **PASS** (tests 75-104 fijan rechazos sin escritura).
- Única nota (S4, 🔵): el JSDoc promete "nunca rechaza" pero el método SÍ puede rechazar (su propio test lo afirma para IDB caído). Ajustar contrato/comentario en próximo slice para no confundir a los call-sites de F1.4.

## Aceptación

- [x] Contexto persiste entre reinicios (simulación con harness fresco sobre mismo IDB+LS)
- [x] Recuperación ante id inválido/borrado
- [x] Rechazos validando antes de escribir
- [x] Flag OFF ⇒ null siempre
- [x] Tests IDB real: 9/9
