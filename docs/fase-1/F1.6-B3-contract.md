# F1.6-B3 — Cloud contract (frozen through B3.5 Unit 3 — B3.5 complete, F1.6-B3 closed)

> **B3.0 froze the cloud contract, audit, and slice design.** B3.4 closed Rules, indexes, and scoped-write admission. B3.5 Unit 1 validated summaries; Unit 2 protected remote admission and manual hydration; **Unit 3 closes LiveSync lifecycle/autostart — F1.6-B3 is now frozen through B3.5 (B3.0→B3.5/U3 closed). No further B3 prod changes without a new F1.6 amendment.**

## Quick path

1. Read the ON/OFF matrix and transition rules before touching B3 cloud paths.
2. For B3.4 Units 2-3 and B3.5 Units 1-3, review the frozen indexes, scoped-write/summary validators, gate-boundary tests, and LiveSync lifecycle (epoch + isCurrent) in §15.
3. B3.5 Unit 3 is closed — LiveSync lifecycle is gated (auth + isProjectsEnabled + captureScopedScope) and epoch-protected; see §15 for wiring, 5 checkpoints, and known limitations.

## 1. ON/OFF matrix (schema2 / schema3 native / promoted-legacy)

| Flag | Store read/write | Repository | Legacy handling | Gate |
|---|---|---|---|---|
| **OFF** | Global (`periodKey`, `closedAtId`, `statusClosedAtId`); no `projectId` filter; legacy schema2 preserved byte-identical | Global queries (`closedAt`/`status+closedAt`/`periodStart+periodEnd`), direct `loadById(id)` | No promotion; no migration | `assertTandaBBlockedWhenScoped` not triggered (`isProjectsEnabled()===false`) |
| **ON** | Project-scoped reads plus validated `importRemote`; general `save`/`saveWithEmployees`/`void` remain blocked | Scoped queries and manual subscription validate captured ownership and deterministic identity | No bulk migration; lazy promotion only (default project) | Protected Sync reads/import/manual subscription are open; `Sync.record`, LiveSync lifecycle, and unrelated Tanda B paths remain blocked |

Schema variants:

- **schema2**: legacy, no `projectId`/`identityKind`/`ownershipToken`; `id = stableToken(fingerprint|after:predecessor)`.
- **schema3 native**: `projectId` canonical mandatory, `schemaVersion===3`, `id = stableToken(fingerprint|project:pid|after:predecessor)`, `fingerprint = JSON(snapshot{projectId,periodStart,periodEnd,rows})`.
- **schema3 promoted-legacy**: `schemaVersion===3`, `projectId=defaultCanonical`, `identityKind==='promoted-legacy'`, `ownershipToken=stableToken(id|fingerprint|project:pid)`, `id` unchanged (legacy id retained).

## 2. Legacy discovery strategy

Adding `where('projectId','==',pid)` on day 1 hides every schema2 row (no field). Therefore:

- **Do not** switch repository to scoped queries until discovery is fenced.
- **Default canonical only**: discovery/promotion applies solely to the default canonical `projectId` (the single project that inherits legacy data). Non-default projects A/B must not discover schema2; they start empty.
- **No bulk migration**: no cloud-side mass rewrite of schema2->schema3. Promotion is lazy, metadata-only, on read when the default project loads (B3.2 local stamper + B3.3 cloud lazy path).
- **No mixing native results**: a scoped query must not return schema2 rows re-labeled as scoped; schema2 rows stay invisible to scoped queries until promoted, then appear as `promoted-legacy`.

## 3. Transition rules

| Transition | Allowed | Notes |
|---|---|---|
| schema2 -> promoted-legacy | YES (metadata-only) | `promoteLegacyPayrollClosure(closure, defaultPid)` only; preserves `id`/`fingerprint`/`rows`/`totals`; sets `projectId`+`identityKind`+`ownershipToken`; re-promotion idempotent, owner mismatch throws. |
| schema2 -> schema3 native | NO | Native requires recomputed fingerprint+id; forbidden. |
| schema3 -> schema2 | FORBIDDEN | Downgrade never allowed; guards must reject. |
| schema3 owner change | FORBIDDEN | `promoteLegacyPayrollClosure` on already-promoted with different `projectId` throws; `voidPayrollClosure` asserts `assertPromotedLegacyClosure` invariant. |

## 4. Rules model (wildcard bypass) — B3.4 Unit 1 applied

Before B3.4 Unit 1, `firestore.rules:6` used `match /users/{userId}/{document=**}`. Firestore Rules matches are additive, so that pattern granted the broad account-owner allow to **every** subcollection, including `payrollClosures`, regardless of any narrower rule.

Unit 1 removes the overlapping user wildcard and gives `payrollClosures` its own match. The current application paths are explicitly owner-scoped: `data`, `employees`, `positions`, `leaders`, `attendance`, `snapshots`, `projects`, `cashPeriods`, `pettyCash`, `projectRegistryV1`, `projectsV1`, and `projectAliasesV1`. Unknown or nested user paths remain denied by the default rule. `mainSyncOutbox` is IndexedDB-only and `receipts` is Cloud Storage, so neither is a Firestore match.

The payroll match is owner-scoped for reads/deletes and validates server-visible document shape for creates/updates. Rules do not inspect `isProjectsEnabled()`, `localStorage`, or the client project registry. Schema2 remains an owner-compatible legacy shape; schema3 requires a non-empty `projectId` that is not the `legacy-unresolved:*` sentinel. Promoted legacy documents require the `promoted-legacy` marker and a non-empty `ownershipToken`, but Rules do not claim to recompute the opaque `stableToken` value.

`isCanonicalProjectId()` intentionally checks only the server-expressible non-empty string and forbidden-sentinel boundary. It does not trim or normalize client input. Exact whitespace normalization remains a client/model concern for B3.4 Unit 3 and is not fabricated as a Rules capability here.

Runtime Rules coverage remains unavailable: this repository has no Firestore emulator configuration or `rules-unit-testing` dependency. Unit 1 therefore uses static Rules-contract assertions and does not claim runtime allow/deny results.

### 4.1 Firestore Rules truth table — B3.1 freeze, Unit 1 enforcement

| # | Operation | Condition | Expected | Rationale / validation |
|---|-----------|-----------|----------|------------------------|
| 1 | create — schema2 legacy | `schemaVersion==2`, no `projectId`, owner `request.auth.uid==userId`, non-empty server-visible `id`/`fingerprint` | **ALLOW** | Preserve OFF contract; Rules cannot recompute the opaque legacy `stableToken` derivation |
| 2 | create — schema3 | `schemaVersion==3`, `projectId` present and canonical-shaped (non-empty, not `legacy-unresolved:*`) | **ALLOW only if canonical-shaped** | Reject if missing/invalid `projectId`; use a server-visible shape check, not self-comparison |
| 3 | update — schema2 → promoted-legacy | existing `schemaVersion==2` → `request.resource.data` has `schemaVersion==3`, `identityKind=='promoted-legacy'`, preserves identity/payload fields and sets canonical-shaped `projectId` + non-empty `ownershipToken` | **ALLOW (only this promotion path)** | Rules preserve fields and marker shape; they do not recompute the opaque token |
| 4 | update — schema3 → schema2 | `resource.data.schemaVersion==3` → `request.resource.data.schemaVersion==2` | **REJECT** | Downgrade never allowed |
| 5 | update — schema3 owner change | `resource.data.projectId` exists and `request.resource.data.projectId != resource.data.projectId` | **REJECT** | Owner immutable; correct check is `request.resource.data.projectId == resource.data.projectId`, NOT tautology `projectId == request.resource.data.projectId` |
| 6 | update — closed → voided | `resource.data.status=='closed'` → `request.resource.data.status=='voided'` with `voidedAt`/`voidedBy`/`voidReason` | **ALLOW** | Legitimate annulment |
| 7 | update — voided → closed | `resource.data.status=='voided'` → `request.resource.data.status=='closed'` | **REJECT** | Irreversible void |
| 8 | wildcard bypass | `match /users/{userId}/{document=**}` granting `payrollClosures` as alternative allow | **MUST NOT** | Generic wildcard must exclude `payrollClosures` or be restructured so `payrollClosures` has its own scoped match; otherwise any `projectId` bypass succeeds |

Notes: the tautology `projectId == request.resource.data.projectId` is invalid (always true on create). Unit 1 uses `isCanonicalProjectId(request.resource.data.projectId)` for the server-visible create shape and `request.resource.data.projectId == resource.data.projectId` for update immutability. Neither the client feature flag nor `stableToken` is evaluated by Rules.

## 5. loadById semantics and owner-check

Today `PayrollClosureRepository.loadById(id):138` does `getDoc(doc(db,'users',uid,'payrollClosures',id))` with no `projectId` check. `PayrollClosureSync.pullDetail(id):93` then `localStore.save(closure)` ingests that document directly.

Leak: caller in project A can `pullDetail(idFromB)` (id guessed/shared via summary) and persist B's detail locally as A — violates §1 invariant.

Frozen contract: `loadById` remains direct document access. B3.3 adds owner validation (check `doc.projectId===capturedPid` or reject) before `save`; tests cover the former leak surface.

## 6. LiveSync lifecycle — closed in B3.5 Unit 3

`closureSummary()` (`PayrollClosureRepository.js`) returns the existing summary fields plus `projectId`, `identityKind`, and `ownershipToken`. Native schema3 summaries preserve the established absence of promotion metadata as `identityKind: null` and `ownershipToken: null`; promoted legacy summaries preserve their stored deterministic metadata. Schema2 summaries expose null scoped metadata and remain excluded from scoped results.

Repository and Sync manual subscriptions are protected public read paths. **B3.5 Unit 3 closes LiveSync**: `app.js` now wires `PayrollClosureLiveSync.start(` gated by `auth.currentUser + isProjectsEnabled() + captureScopedScope()`, subscribes to `projectContext.subscribe` for project switches, and stops on logout/switch/OFF. `PayrollClosureLiveSync` owns an `epoch` counter and `isCurrent` predicate; `PayrollClosureSync.subscribeRecent` enforces `isCurrent` at 5 checkpoints (before batch, after each `loadById`, before/after `importClosures`, before `onApply`) and silently drops `PAYROLL_CLOSURE_SYNC_CANCELLED` / `PAYROLL_CLOSURE_STALE_READ` without surfacing to `onError` or `onApply`. Unit 2 hydration/validation (summary→detail→assertSummaryMatchesDetail→import) remains; Unit 3 adds only lifecycle/generation, no new query shape.

### 6.1 closureSummary frozen target (B3.1, implemented in B3.5 Unit 1)

Target `closureSummary` is a superset of current fields plus at minimum `projectId`, `identityKind`, and `ownershipToken`. `validatePayrollClosureSummaryForScopedRead()` checks only summary-available identity fields: numeric schema3, canonical captured owner, identity kind, deterministic ID, and promoted token. It deliberately does not inspect rows or totals. Schema2 summaries are rejected by the validator and are never presented by the scoped subscription.

## 7. Queries and indexes

Current after B3.4 Unit 2: `firestore.indexes.json` has five `payrollClosures` collection indexes: the preserved legacy `status ASC, closedAt DESC, __name__ DESC` index plus the four scoped composites below.

Required (justified):

- `projectId ASC, closedAt DESC, __name__ DESC` — pagination `listPage`/`loadPage` per project.
- `projectId ASC, status ASC, closedAt DESC, __name__ DESC` — filtered pagination.
- `schemaVersion ASC, closedAt DESC, __name__ DESC` — schema-aware pagination across legacy and promoted/native records.
- `schemaVersion ASC, status ASC, closedAt DESC, __name__ DESC` — schema-aware filtered pagination.

Provisional (not added until proven — B3.2 outcome: not needed for equality-only, kept out of `firestore.indexes.json`):

- `projectId ASC, periodStart ASC, periodEnd ASC` (or with `closedAt`) — `loadByPeriod` currently uses two `where` without ordering; stays without composite until a real `FAILED_PRECONDITION` test reproduces the need. Do not add speculatively.
- **B3.2 preflight finding**: `where(projectId==pid)+where(periodStart==X)+where(periodEnd==Y)` is **three equality filters, no range, no `orderBy`** → Firestore merges single-field indexes (see [index overview — use index merging](https://firebase.google.com/docs/firestore/query-data/index-overview) + docs `where(projectId==)+where(periodStart==)+where(periodEnd==)` = no composite). Composite is only required when adding range/inequality or `orderBy` on a different field. See §11.

## 8. Capture-before-await / stale completion and ABA

`PayrollClosureStore` contract (`PayrollClosureStore.js:42,51`):

- `captureScopedProjectId()` captures `pid` from `captureEntityProjectScope()` **before** any `await` (`db.get`/`query`/`getPageByIndex`).
- `ensureNotStale(pid)` re-checks `peekEntityScope()` **after** each await; if `!enabled` or `curPid!==pid` throws `PAYROLL_CLOSURE_STALE_READ`.
- `getById`/`getByPeriod`/`listPage`/`getSyncStates` all follow capture -> await -> ensureNotStale -> ownsClosure.

No floating promise may complete into a stale project. Repository scoped reads use the same capture-before-await and stale-completion guard.

**Stale/ABA final state (B3.5 Unit 3):**

- Direct `pullPage`/`pullDetail`/`pullPeriod`/`importClosures` paths remain capture-before-await + ensureNotStale; an `A→B` switch throws `PAYROLL_CLOSURE_STALE_READ` and does not persist.
- LiveSync adds a second layer: `PayrollClosureLiveSync` owns a monotonic `epoch`; `start()` increments epoch and `stop()` increments epoch and calls unsubscribe. `PayrollClosureSync.subscribeRecent` receives `isCurrent` (epoch equality) and checks it at 5 checkpoints (before batch, after each `loadById`, before `importClosures`, after `importClosures`, before `onApply`). On `isCurrent===false` it throws `PAYROLL_CLOSURE_SYNC_CANCELLED`, which is silenced (no `onError`, no `onApply`).
- This closes `A→B`, `A→B→A`, logout-during-`loadById`/import, double-start (at most one active subscription), and straddling hydration (summary batch is validated; mixed-project batches fail before import).
- Residual Store `A→B→A` via plain `ensureNotStale(pid equality)` is mitigated at the LiveSync boundary by epoch; the Store itself still compares only `pid` equality (no generation) — see §15.3 known limitation for the deliberate non-generation choice.

## 9. B3.1 -> B3.5 subdivision

| Slice | Scope | Acceptance criteria | Forbidden |
|---|---|---|---|
| **B3.1** | Contract test hardening + `closureSummary` shape proposal (doc only) | `PayrollClosureB3Contract` green; no prod change | Modifying `firestore.rules`, indexes, repository queries |
| **B3.2** | Preflight: resolve period index need + emulator verifiability + preserve B3.4 Rules nuance (doc+tests only, no prod query) | `PayrollClosureB3Contract` + `PayrollClosureB3Preflight` green; `firestore.indexes.json` stays legacy-only; provisional `projectId+periodStart+periodEnd` NOT added (§11) | Cloud writes, LiveSync activation, modifying `firestore.indexes.json` speculatively, adding production query code |
| **B3.3** | Repository scoping + `loadById` owner-check + lazy promotion read path | `where(projectId==pid)` with default-only legacy fallback; `loadById` rejects cross-owner; no schema2 exposed as scoped; public ON gates remain | Bulk migration, `projectId+periodStart+periodEnd` index without FAILED_PRECONDITION |
| **B3.4 Unit 1** | `firestore.rules` restructure and payroll wildcard boundary | Rules deny `payrollClosures` via the removed wildcard; known direct user paths remain owner-scoped; static security tests green | Index changes, client validators, gate removal, mixing native+legacy results, LiveSync activation |
| **B3.4 Unit 2** | Scoped `payrollClosures` indexes | Five collection indexes present: four scoped composites plus the preserved legacy index; period composite remains absent | Rules changes, client validators, gate removal, LiveSync activation |
| **B3.4 Unit 3** | Client/model validation and authorized gate changes | Strict native/promoted scoped-write admission; Repository `saveOne`/`loadPage`/`loadByPeriod`/`loadById` gates removed; `subscribeRecent` and Sync gates remain | LiveSync activation, schema migration, weakening Rules boundary |
| **B3.5 Unit 1** | Enriched summary identity and internal scoped subscription query | `closureSummary` includes `projectId`/`identityKind`/`ownershipToken`; native/promoted summary admission validates ownership without rows/totals; `subscribeRecentScoped` captures `projectId`, keeps ordering/limit, filters schema2, and drops stale callbacks; public subscription and Sync gates remain | `importClosures` gate removal, LiveSync activation, owner change of schema3, schema3->schema2 |
| **B3.5 Unit 2** | Protected remote admission and manual full-detail hydration | Full identity/owner validation at Sync and Store boundaries; stale completion cannot write; `assertSummaryMatchesDetail` enforces immutable identity | LiveSync lifecycle/autostart, general Store writes |
| **B3.5 Unit 3** | LiveSync lifecycle/autostart (closed) | Gated autostart on login + project switch + auth switch; `epoch` + `isCurrent` at 5 checkpoints; `PAYROLL_CLOSURE_SYNC_CANCELLED` silent; at most one active subscription; no `onApply` residual across `A→B`, `A→B→A`, logout-during-load/import, double-start | New query shapes, Rules/indexes changes, bulk migration, weakening admission |

Each slice is a <400-line work unit with its own tests; rollback is slice-exclusive.

## 10. Verification (frozen — B3.5 complete)

- `npm test -- PayrollClosureB34Rules --runInBand` — static Unit 1 Rules boundary and runtime-harness limitation.
- `npm test -- PayrollClosureB3Contract --runInBand` — contract audit (global queries, direct loadById, summary shape, Rules boundary, OFF→scoped transition, gated LiveSync, stale/epoch guard, indexes).
- `npm test -- PayrollClosureB3Preflight --runInBand` — B3.2/Unit 2 index contract (five query-shape indexes, provisional period-index absence, emulator non-enforcement, Rules verifiability nuance).
- `npm test -- PayrollClosure.test.js PayrollClosureSync.test.js PayrollClosureStoreB2.2.test.js TandaBGates.test.js --runInBand` — scoped summary/detail/import admission, stale persistence, gates, and OFF compatibility.
- `npm test -- PayrollClosureLiveSyncU3 --runInBand` — U3-B/C lifecycle: start→stop cancellation, A→B, A→B→A (epoch), double-start single subscription, logout-during-load/import, 5 checkpoints, real error vs silent cancellation.
- `npm test -- PayrollClosureU3DMatrix --runInBand` — U3-D matrix through Repository+Sync+Store: native/promoted/legacy discovery, A/B isolation, pagination, straddling blocked, void/retry idempotence, mixed-batch rejection, OFF exact.
- `npm test -- PayrollClosure --runInBand` — smoke: no regression on existing closure suites.
- `npm test -- --runInBand` — full suite (B3 + app wiring + pre-commit convergence).
- `git diff --check` and authored additions+deletions must remain under 400; `firestore.rules` and `firestore.indexes.json` remain unchanged in U3-E (no Rule/index prod change).
- Docs: `docs/fase-1/F1.6-B3-contract.md` is the frozen closure artifact (B3.0→B3.5/U3) — see §§6,8,9,15,16.

## 11. B3.2 preflight — equality-only index + emulator + Rules verifiability (no productive change)

> B3.2 is doc+tests only. No `PayrollClosureRepository`, `firestore.rules`, or `firestore.indexes.json` mutation. Findings frozen here so B3.4 does not lose nuance.

### 11.1 Index need: does `projectId==pid + periodStart==X + periodEnd==Y` require composite?

**Resolved: NO — not with current shape.**

- Current `loadByPeriod` is `where('periodStart','==',…)+where('periodEnd','==',…)`, no `orderBy`. Future scoped form would be `where('projectId','==',pid)` + same two equalities, still **three equality filters, no range, no `orderBy`** (see `PayrollClosureRepository.js:145`).
- Firestore merges single-field indexes for equality-only queries: "[Can] re-use existing indexes … merge the indexes for simple equality filters to build the composite indexes needed for larger equality queries" — [index overview](https://firebase.google.com/docs/firestore/query-data/index-overview). Equality → no dedicated composite; composite required only with range/inequality or `orderBy` on different field.
- Therefore `projectId ASC, periodStart ASC, periodEnd ASC` (or with `closedAt`) is **provisional and NOT added** to `firestore.indexes.json` in B3.2. Adding it "just in case" would violate work-unit rule and incur spurious storage/maintenance.
- If B3.3/B3.4 later adds `orderBy('closedAt')` or a range filter to `loadByPeriod`, the need must be reproven with a real `FAILED_PRECONDITION` reproduction against production (or emulator with enforcement) before adding any index.

### 11.2 Emulator / `FAILED_PRECONDITION` reproduction attempt

Attempted deterministic repro: query `where('projectId','==',pid)+where('periodStart','==',X)+where('periodEnd','==',Y)` and expect `code === 'failed-precondition'` with index-creation URL on missing index (production behavior).

**Finding: not reliably reproducible in this repo’s environment.**

- Official emulator contract: "The emulator does not track composite indexes and will instead execute any valid query. Make sure to test your app against a real Firestore instance to determine which indexes you require." — [Use Firestore emulator locally — Indexes](https://cloud.google.com/firestore/native/docs/emulator#indexes) and [Test Datastore Mode — Indexes](https://docs.cloud.google.com/datastore/docs/emulator#indexes). Same note in bug reports and community docs: emulator by default never throws `FAILED_PRECONDITION` for missing composite index.
- Some setups expose `FIRESTORE_EMULATOR_ENABLE_INDEX_ENFORCEMENT` / REST index creation, but this repo has no emulator harness, no `firebase.json` emulator config requiring indexes, and `js/tests` mock Firestore via `jest` mappers — so no deterministic `FAILED_PRECONDITION` surface exists.
- Decision: the equality-only period index remains **provisional and absent from `firestore.indexes.json`** (see §7). The `PayrollClosureB3Preflight` test documents this emulator limitation explicitly and asserts the five query-shape indexes without fabricating a fake failure.

### 11.3 Rules verifiability nuance applied in B3.4 Units 1-3

Rules cannot know client-only feature flag — B3.4 Units 1-3 must not claim Rules branches on `isProjectsEnabled()`.

B3.1 truth table (§4.1) remains the semantic contract. Units 1-3 preserve its Firestore-expressible boundary; Unit 3 adds client admission without claiming extra Rules capabilities:

| Concern | Nuance to preserve | Implication for B3.4 |
|---------|-------------------|----------------------|
| Client-only feature flag (`isProjectsEnabled()` / schema2 OFF) | Rules cannot read client memory/offline flag or `localStorage`. Row "create schema2 OFF" has no server-visible signal; Rules sees only the document shape. | **Do not claim Rules knows ON/OFF.** In B3.4 either (a) define a server-visible signal (e.g., `schemaVersion==2` as legacy owner path, distinct from `schemaVersion==3` scoped path) or (b) accept schema2 as legacy owner path (ALLOW if `request.auth.uid==userId` + valid legacy identity), without branching on client flag. Doc must state this explicitly. |
| `defaultCanonical` / `promoted-legacy` ownership | `defaultCanonical` is client-derived (canonical project list); Rules has no access to project registry. `ownershipToken == stableToken(id|fingerprint|project:pid)` is opaque client hash. | Only enforce what Firestore can verify: `projectId` is non-empty canonical-shape (e.g., `isValidProjectId` = non-empty string, not `legacy-unresolved:*`), `identityKind == 'promoted-legacy'` presence, and immutability `request.resource.data.projectId == resource.data.projectId`. Do **not** attempt to recompute `stableToken` server-side unless a verifiable derivation is exposed; if not verifiable, document as client contract with server-side immutability guard. |
| `defaultCanonical` / `ownershipToken` validation | Over-validating unverifiable tokens would reject legitimate legacy promotions; under-validating would accept spoofed claims. | B3.4 must phrase validations as "only if Firestore-expressible and verifiable" — enforce non-empty `projectId`, canonical shape, and preservation of `id`/`fingerprint` on promotion; treat `ownershipToken` as integrity hint, not as recomputable proof, unless Firestore function can reproduce it. |

Preserved literal keys for search: `defaultCanonical/ownershipToken` — only enforce if Firestore-expressible and verifiable.

If B3.4 over-claims Rules awareness of client flag or recomputability of `ownershipToken`, the contract silently diverges from enforceable reality. This appendix is the source of truth for that constraint.

### 11.4 Provisional-index decision (frozen)

- `firestore.indexes.json` was **legacy-only** (`status+closedAt+__name__`) in B3.2 — verified by the B3.2 preflight.
- B3.4 Unit 2 adds `projectId+closedAt+__name__`, `projectId+status+closedAt+__name__`, `schemaVersion+closedAt+__name__`, and `schemaVersion+status+closedAt+__name__` for the implemented scoped query shapes.
- `projectId+periodStart+periodEnd` remains **provisional, absent** — may be added only after a real `FAILED_PRECONDITION` repro if query shape gains `orderBy`/range. Must not be invented "just in case".

## 12. B3.4 Unit 3 — scoped write admission and gate boundary

Unit 3 is complete only when all of the following remain true:

- `validatePayrollClosureForScopedWrite()` is the model admission point before `saveOneScoped()` enters a cloud transaction.
- Native schema3 requires an exact canonical `projectId`, numeric `schemaVersion === 3`, Rules-required fields, finite numeric/integer values without coercion, canonical snapshot fingerprint, deterministic project-bound ID, and matching financial totals.
- Promoted legacy requires `identityKind == 'promoted-legacy'`, canonical immutable owner metadata, the legacy-derived ID/token relationship, and the Rules-required fields. When a schema2 source is available, the promotion must equal the metadata-only result from `promoteLegacyPayrollClosure()` so `id`, `fingerprint`, `supersedesId`, rows, totals, payment references, and historical payload remain unchanged.
- `ownershipToken` is checked against the existing deterministic client helper where available, but it is not described as cryptographic proof. Firestore Rules still only enforce its non-empty shape and immutable ownership boundary.
- Legacy schema2 discovery and default-only lazy promotion remain unchanged and do not use the strict native payload validator.

### 12.1 B3.4 complete boundary

| Path | Projects ON behavior |
|---|---|
| `PayrollClosureRepository.saveOne` | Released; strict native/promoted validator runs before transaction admission |
| `PayrollClosureRepository.loadPage` | Released; captured project query and stale completion guard remain |
| `PayrollClosureRepository.loadByPeriod` | Released; project equality and default-only legacy fallback remain |
| `PayrollClosureRepository.loadById` | Released; cross-owner detail remains rejected before local ingestion |
| `PayrollClosureRepository.subscribeRecent` | **Blocked** until B3.5 summary ownership is enriched |
| `PayrollClosureSync.*` | **Blocked**; no public Sync path is opened in Unit 3 |
| `PayrollClosureLiveSync` / app autostart | **Dormant**; B3.5 only |

Unit 3 removes the Repository saveOne/loadPage/loadByPeriod/loadById gates; Sync and LiveSync gates remain blocked.

This is the **B3.4 complete boundary**: B3.4 Units 1-3 are implemented. No Rules or index changes are part of Unit 3.

## 13. B3.5 Unit 1 — summary identity and scoped query boundary

Unit 1 is complete only when all of the following remain true:

- `closureSummary()` preserves every prior summary field and adds scoped identity metadata. Schema2 summaries are explicitly non-scoped; native schema3 summaries preserve the established null promotion metadata; promoted legacy summaries preserve the stored marker and deterministic token.
- `validatePayrollClosureSummaryForScopedRead()` rejects schema2, missing or non-canonical ownership, forged native IDs, invalid native metadata, and invalid promoted metadata. It does not claim to validate rows or totals that are absent from a summary.
- `subscribeRecentScoped()` uses the captured canonical `projectId` in its Firestore query, preserves `closedAt`/document-ID descending order and bounded limit, validates admitted summaries, filters schema2, and rejects callbacks after project switch or Projects OFF.
- The public `PayrollClosureRepository.subscribeRecent` gate, every `PayrollClosureSync` gate, and LiveSync dormancy remain unchanged. No `importClosures` activation is included.

B3.5 Unit 2 is frozen below. B3.5 Unit 3 is closed in §15 — no separate authorization remains.

## 14. B3.5 Unit 2 — protected remote admission

- Scoped page summaries use `validatePayrollClosureSummaryForScopedRead()`; details, period pulls, imports, and Store persistence use `validatePayrollClosureForScopedWrite()`. Schema2 is never admitted as scoped outside Repository promotion.
- Sync validates complete batches before writes. `PayrollClosureStore.importRemote()` revalidates and checks the captured scope before, inside, and after its atomic mutation while preserving merge retry, duplicate, conflict, and monotonic void semantics.
- `pullPage`, `pullDetail`, `pullPeriod`, `importClosures`, and manual `subscribeRecent` are open. `Sync.record` and general Store economic writes remain blocked; OFF behavior remains legacy-compatible.
- Manual subscription captures one scope, validates summaries, hydrates each full detail, compares immutable identity fields without requiring equal status, then imports. Summary-only persistence is forbidden.
- Direct A→B stale completion is rejected. LiveSync `A→B→A` ABA is closed by the Unit 3 epoch (see §8, §15); the Store's plain pid-equality check without generation remains as documented in §15.3.

## 15. B3.5 Unit 3 — LiveSync lifecycle/autostart (closed)

Unit 3 is complete only when all of the following remain true:

- **Wiring (`app.js`)** — `PayrollClosureLiveSync` is gated by `auth.currentUser && isProjectsEnabled() && captureScopedScope()` (`_canStartPayrollLiveSync`), started on login after `claimLocalOwnership`/`initProjectsInfrastructure` via `_attemptPayrollLiveSync()`, subscribed to `projectContext.subscribe` (project switch restarts with new epoch), and stopped on `auth` switch/logout (`_prevUid/_nextUid` path) and when gating fails. `OFF` never keeps LiveSync active.
- **LiveSync module (`PayrollClosureLiveSync.js`)** — owns monotonic `epoch`; `start()` calls `stop()` then captures `myEpoch = epoch` and `isCurrent = () => myEpoch === epoch`, passes `isCurrent` into `payrollClosureSync.subscribeRecent`; `stop()` increments `epoch`, calls unsubscribe, clears handle; `isActive()` reflects subscription presence; double `start()` keeps at most one valid subscription (first unsub called, second active).
- **Sync generation guard (`PayrollClosureSync.js:subscribeRecent`)** — captures `scope` via `captureRemoteScope()` and `isCurrent` from options; checks `isCurrent` at **5 checkpoints**: before batch, after each `loadById`, before `importClosures`, after `importClosures` (plus stale scope check), before `onApply`; on `!isCurrent` throws `PAYROLL_CLOSURE_SYNC_CANCELLED`; `isSilent` swallows `PAYROLL_CLOSURE_SYNC_CANCELLED` and `PAYROLL_CLOSURE_STALE_READ` so neither `onError` nor `onApply` leaks cancellation; real errors still reach `onError`.
- **Hydration** — summary batch validated via `validatePayrollClosureSummaryForScopedRead`; each `loadById` detail validated via `validatePayrollClosureForScopedWrite`; `assertSummaryMatchesDetail` compares immutable identity fields (`schemaVersion`, `id`, `fingerprint`, `projectId`, `identityKind`, `ownershipToken`, `periodStart`, `periodEnd`, `supersedesId`) without requiring equal `status`; then `importClosures` re-validates before `Store.importRemote`.
- **Matrices verified by `PayrollClosureLiveSyncU3.test.js` (U3-B/C) and `PayrollClosureU3DMatrix.test.js` (U3-D)**: `A→B` pending only `B` applies; `A→B→A` with pending ops only `A2` may apply; unsubscribe/restart no residual `onApply`; mixed-batch (hostile `projectId`, forged `id`, schema2, tampered `ownershipToken`) rejected before any import; pagination does not leak `B` into `A`; straddling hydration blocked; void/retry duplicate idempotent; OFF exact (global queries return both projects, no isolation).

### 15.1 B3.5 complete boundary

| Path | Projects ON behavior (final) |
|---|---|
| `PayrollClosureRepository.saveOne` | Released (B3.4 U3); strict native/promoted validator |
| `PayrollClosureRepository.loadPage` / `loadByPeriod` / `loadById` | Released; scoped query + legacy default fallback + cross-owner reject |
| `PayrollClosureRepository.subscribeRecent` (scoped) | **Released via LiveSync**; captured scope, ordering/limit preserved, schema2 filtered |
| `PayrollClosureSync.pullPage` / `pullDetail` / `pullPeriod` / `importClosures` | **Released (B3.5 U2)**; full admission, batch atomic |
| `PayrollClosureSync.subscribeRecent` (manual) | **Released (B3.5 U2)**; hydrated full-detail import, silent stale/cancel |
| `PayrollClosureLiveSync` / app autostart | **Released (B3.5 U3)**; gated, epoch-protected, projectContext-aware |
| `PayrollClosureSync.record` / general Store writes | **Blocked**; Tanda B remains blocked under scoped mode |
| `firestore.rules` / `firestore.indexes.json` | **Frozen**; no prod change in U3-E (see §16) |

## 16. B3.5 close — frozen matrices, final verification, and known limitations

### 16.1 Frozen slices and rollback

B3.0→B3.5/U3 is closed: B3.1 (contract), B3.2 (preflight), B3.3 (scoping + owner-check + promotion), B3.4 U1 (Rules wildcard), U2 (indexes), U3 (admission + gates), B3.5 U1 (summary), U2 (protected admission), U3 (LiveSync lifecycle). Each slice remains a <400-line work unit with slice-exclusive rollback; reverting U3 removes `PayrollClosureLiveSync` epoch + `isCurrent` wiring and `app.js` `projectContext.subscribe`/autostart without touching B3.4 indexes/rules or B3.5 summary/admission.

### 16.2 Final verification (U3-E)

- `npm test -- --runInBand` — **full suite passes** (B3 suites + AppWiring + PreCommitConvergence).
- `node scripts/check-state-writes.mjs` — no new direct `state` writes (ratchet).
- `git diff --check` — no whitespace errors.
- Normalizer: `pre-commit` hook is idempotent — generate/stage then run again preserves exact staged tree and diff (proven by `PreCommitConvergence`); no `firestore.rules`/`indexes` prod change in U3-E.

### 16.3 Known limitations (frozen — not fabricated as fixed)

- **ON→OFF gap**: switching Projects `ON→OFF` while a scoped `loadPage`/`pullPage`/`subscribeRecent` is in-flight may complete with `PAYROLL_CLOSURE_STALE_READ` (Store) or silent `CANCELLED` (LiveSync) and not apply; however global (OFF) re-reads after the switch are unscoped and correctly return both projects. No automatic re-hydration across the flag flip is provided — caller re-issues the OFF query. This gap is intentional and documented; no generation is added to OFF.
- **Firebase Rules / indexes not validated against a real backend**: `firestore.rules` and `firestore.indexes.json` are asserted statically (truth table §4.1, 5 indexes, provisional period composite absent). No `rules-unit-testing` harness or `FIRESTORE_EMULATOR_ENABLE_INDEX_ENFORCEMENT` repro exists in this repo; `FAILED_PRECONDITION` is not reliably reproducible via emulator (see §11.2). Runtime allow/deny and missing-index errors must be reproven against a real Firestore instance before claiming enforcement.
- **No indirect cross-project ingest via LiveSync (verified)**: `subscribeRecent` validates every summary (`validatePayrollClosureSummaryForScopedRead`) and every hydrated detail (`validatePayrollClosureForScopedWrite` + `assertSummaryMatchesDetail`) before `importClosures`; mixed-project, forged-id, schema2, and tampered-token batches are rejected before any `Store.importRemote`; pagination straddling is blocked; `U3DMatrix` proves A summaries cannot hydrate B details and vice-versa. LiveSync never does summary-only persistence.
- **Store `A→B→A` without generation**: the Store itself still checks only `curPid!==pid` (no generation counter). Rapid `A→B→A` via plain Store `importRemote` could pass the Store's stale check if the pid returns to `A` before `ensureNotStale`. LiveSync's `epoch` closes this at the subscription boundary (only latest epoch may `onApply`); plain Store ABA remains a documented residual for non-LiveSync Store callers — acceptable because all cloud ingest in B3.5 goes through the epoch-guarded LiveSync/Sync path.
