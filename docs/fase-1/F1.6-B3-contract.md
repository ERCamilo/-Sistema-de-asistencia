# F1.6-B3 — Cloud contract (frozen, no behavior change)

> **B3.0 freezes the cloud contract, audit, and slice design. No productive behavior change.** All code, rules, and indexes remain at `fd8dc7e`. B3.1-B3.5 are the only authorized mutators.

## Quick path

1. Read ON/OFF matrix and transition rules before touching `PayrollClosureRepository`, `PayrollClosureSync`, `PayrollClosureStore`, `firestore.rules`, or `firestore.indexes.json`.
2. Run `npm test -- PayrollClosureB3Contract --runInBand` — it asserts current (pre-B3) behavior stays green.
3. Implement B3.1-B3.5 only in declared order; each slice must pass its acceptance table.

## 1. ON/OFF matrix (schema2 / schema3 native / promoted-legacy)

| Flag | Store read/write | Repository | Legacy handling | Gate |
|---|---|---|---|---|
| **OFF** | Global (`periodKey`, `closedAtId`, `statusClosedAtId`); no `projectId` filter; legacy schema2 preserved byte-identical | Global queries (`closedAt`/`status+closedAt`/`periodStart+periodEnd`), direct `loadById(id)` | No promotion; no migration | `assertTandaBBlockedWhenScoped` not triggered (`isProjectsEnabled()===false`) |
| **ON** | Project-scoped (`projectId`, `projectClosedAtId`, `projectStatusClosedAtId`); `captureScopedProjectId()`+`ensureNotStale()`; `ownsClosure()` filters | **Contract frozen still-global** until B3.3 — adding `where(projectId==pid)` now would hide schema2 rows (see §2) | No bulk migration; lazy promotion only (default project) | Tanda B blocked (`PayrollClosureStore.save`, `PayrollClosureRepository.*`, `PayrollClosureSync.*`) — until B3 opens slice-by-slice |

Schema variants:

- **schema2**: legacy, no `projectId`/`identityKind`/`ownershipToken`; `id = stableToken(fingerprint|after:predecessor)`.
- **schema3 native**: `projectId` canonical mandatory, `schemaVersion===3`, `id = stableToken(fingerprint|project:pid|after:predecessor)`, `fingerprint = JSON(snapshot{projectId,periodStart,periodEnd,rows})`.
- **schema3 promoted-legacy**: `schemaVersion===3`, `projectId=defaultCanonical`, `identityKind==='promoted-legacy'`, `ownershipToken=stableToken(id|fingerprint|project:pid)`, `id` unchanged (legacy id retained).

## 2. Legacy discovery strategy

Adding `where('projectId','==',pid)` on day 1 hides every schema2 row (no field). Therefore:

- **Do not** switch repository to scoped queries until discovery is fenced.
- **Default canonical only**: discovery/promotion applies solely to the default canonical `projectId` (the single project that inherits legacy data). Non-default projects A/B must not discover schema2; they start empty.
- **No bulk migration**: no cloud-side mass rewrite of schema2->schema3. Promotion is lazy, metadata-only, on read when the default project loads (B3.2 local stamper + future B3.3 cloud lazy path).
- **No mixing native results**: a scoped query must not return schema2 rows re-labeled as scoped; schema2 rows stay invisible to scoped queries until promoted, then appear as `promoted-legacy`.

## 3. Transition rules

| Transition | Allowed | Notes |
|---|---|---|
| schema2 -> promoted-legacy | YES (metadata-only) | `promoteLegacyPayrollClosure(closure, defaultPid)` only; preserves `id`/`fingerprint`/`rows`/`totals`; sets `projectId`+`identityKind`+`ownershipToken`; re-promotion idempotent, owner mismatch throws. |
| schema2 -> schema3 native | NO | Native requires recomputed fingerprint+id; forbidden. |
| schema3 -> schema2 | FORBIDDEN | Downgrade never allowed; guards must reject. |
| schema3 owner change | FORBIDDEN | `promoteLegacyPayrollClosure` on already-promoted with different `projectId` throws; `voidPayrollClosure` asserts `assertPromotedLegacyClosure` invariant. |

## 4. Rules model (wildcard bypass) — truth table frozen in B3.1

Current `firestore.rules:6` uses `match /users/{userId}/{document=**}` which matches **every** subcollection including `payrollClosures`. This is a bypass: any `users/{uid}/payrollClosures/{id}` inherits the broad read/write allow.

Contract (not yet applied): exclude `payrollClosures` from the wildcard or restructure matches so `payrollClosures` has its own rule with proper ownership validation (canonical `projectId` + immutability). `firestore.rules` stays unchanged in B3.0/B3.1; tests assert the wildcard still covers `payrollClosures` and that no `payrollClosures`-specific allow exists yet.

### 4.1 Firestore Rules truth table — desired contract (B3.1 freeze, enforcement in B3.4)

| # | Operation | Condition | Expected | Rationale / validation |
|---|-----------|-----------|----------|------------------------|
| 1 | create — schema2 legacy | `schemaVersion==2`, no `projectId`, owner `request.auth.uid==userId`, valid legacy `id`/`fingerprint` | **ALLOW** | Preserve OFF contract; historical rows remain readable/writable by account owner |
| 2 | create — schema3 | `schemaVersion==3`, `projectId` present and canonical valid (non-empty, not `legacy-unresolved:*`) | **ALLOW only if canonical** | Reject if missing/invalid `projectId`; must pass `canonicalProjectId()`-equivalent check, not self-comparison |
| 3 | update — schema2 → promoted-legacy | existing `schemaVersion==2` → `request.resource.data` has `schemaVersion==3`, `identityKind=='promoted-legacy'`, preserves `id`/`fingerprint`/`supersedesId`/`rows`/`totals` (economy) and sets `projectId==defaultCanonical` + `ownershipToken==stableToken(id\|fingerprint\|project:pid)` | **ALLOW (only this promotion path)** | Any other mutation of historical identity rejected |
| 4 | update — schema3 → schema2 | `resource.data.schemaVersion==3` → `request.resource.data.schemaVersion==2` | **REJECT** | Downgrade never allowed |
| 5 | update — schema3 owner change | `resource.data.projectId` exists and `request.resource.data.projectId != resource.data.projectId` | **REJECT** | Owner immutable; correct check is `request.resource.data.projectId == resource.data.projectId`, NOT tautology `projectId == request.resource.data.projectId` |
| 6 | update — closed → voided | `resource.data.status=='closed'` → `request.resource.data.status=='voided'` with `voidedAt`/`voidedBy`/`voidReason` | **ALLOW** | Legitimate annulment |
| 7 | update — voided → closed | `resource.data.status=='voided'` → `request.resource.data.status=='closed'` | **REJECT** | Irreversible void |
| 8 | wildcard bypass | `match /users/{userId}/{document=**}` granting `payrollClosures` as alternative allow | **MUST NOT** | Generic wildcard must exclude `payrollClosures` or be restructured so `payrollClosures` has its own scoped match; otherwise any `projectId` bypass succeeds |

Notes: the tautology `projectId == request.resource.data.projectId` is invalid (always true on create); correct validations are `isValidProjectId(request.resource.data.projectId)` / `canonicalProjectId()` for create and `request.resource.data.projectId == resource.data.projectId` for update immutability. B3.1 documents; B3.4 enforces.

## 5. loadById semantics and owner-check

Today `PayrollClosureRepository.loadById(id):138` does `getDoc(doc(db,'users',uid,'payrollClosures',id))` with no `projectId` check. `PayrollClosureSync.pullDetail(id):93` then `localStore.save(closure)` ingests that document directly.

Leak: caller in project A can `pullDetail(idFromB)` (id guessed/shared via summary) and persist B's detail locally as A — violates §1 invariant.

Frozen contract: `loadById` stays direct. B3.3 must add owner validation (check `doc.projectId===capturedPid` or reject) before `save`; tests assert the leak surface exists.

## 6. LiveSync dormant until B3.5

`closureSummary()` (`PayrollClosureRepository.js:66`) returns `{schemaVersion,id,fingerprint,periodStart,periodEnd,periodSource,status,closedAt,closedBy,updatedAt,totals,employeeCount,undoUntil,supersedesId,voidedAt,voidedBy,voidReason}` — **no `projectId`/`identityKind`/`ownershipToken`**.

`PayrollClosureRepository.subscribeRecent` + `PayrollClosureSync.subscribeRecent` + `PayrollClosureLiveSync.start` therefore cannot enforce ownership. Contract: LiveSync stays dormant (never auto-started; `app.js` has no `PayrollClosureLiveSync.start(`). B3.5 must add `projectId`+`identityKind`+`ownershipToken` to summary and gate `importClosures` before activating.

### 6.1 closureSummary frozen target (B3.1, implementation in B3.5)

Target `closureSummary` is superset of current fields plus at minimum `projectId`, `identityKind`, `ownershipToken` to allow B3.5 ownership validation in `pullDetail`/`subscribeRecent`/`importClosures`. Current code **omits them as found in B3.0 audit** (intentionally preserved in B3.1, no productive change). B3.5 will add them; until then LiveSync stays dormant and no `projectId` filtering is enforced in summaries.

## 7. Queries and indexes

Existing: `firestore.indexes.json` has single composite `status ASC, closedAt DESC, __name__ DESC` on `payrollClosures`.

Required (justified):

- `projectId ASC, closedAt DESC, __name__ DESC` — pagination `listPage`/`loadPage` per project.
- `projectId ASC, status ASC, closedAt DESC, __name__ DESC` — filtered pagination.

Provisional (not added until proven):

- `projectId ASC, periodStart ASC, periodEnd ASC` (or with `closedAt`) — `loadByPeriod` currently uses two `where` without ordering; stays without composite until a real `FAILED_PRECONDITION` test reproduces the need. Do not add speculatively.

## 8. Capture-before-await / stale completion

`PayrollClosureStore` contract (`PayrollClosureStore.js:42,51`):

- `captureScopedProjectId()` captures `pid` from `captureEntityProjectScope()` **before** any `await` (`db.get`/`query`/`getPageByIndex`).
- `ensureNotStale(pid)` re-checks `peekEntityScope()` **after** each await; if `!enabled` or `curPid!==pid` throws `PAYROLL_CLOSURE_STALE_READ`.
- `getById`/`getByPeriod`/`listPage`/`getSyncStates` all follow capture -> await -> ensureNotStale -> ownsClosure.

No floating promise may complete into a stale project. Same pattern will be required for repository when it becomes scoped.

## 9. B3.1 -> B3.5 subdivision

| Slice | Scope | Acceptance criteria | Forbidden |
|---|---|---|---|
| **B3.1** | Contract test hardening + `closureSummary` shape proposal (doc only) | `PayrollClosureB3Contract` green; no prod change | Modifying `firestore.rules`, indexes, repository queries |
| **B3.2** | Local `PayrollClosureStore`+`Stamper` project-awareness already done; add FAILED_PRECONDITION repro for period index | Store tests still green; period index stays provisional with repro test | Cloud writes, LiveSync activation |
| **B3.3** | Repository scoping + `loadById` owner-check + lazy promotion read path | `where(projectId==pid)` with default-only legacy fallback; `loadById` rejects cross-owner; no schema2 exposed as scoped | Bulk migration, `projectId+periodStart+periodEnd` index without FAILED_PRECONDITION |
| **B3.4** | `firestore.rules` restructure + indexes `projectId+closedAt`, `projectId+status+closedAt` | Rules deny `payrollClosures` via wildcard; scoped indexes exist; security tests green | Mixing native+legacy results, LiveSync activation |
| **B3.5** | LiveSync activation with enriched summary | `closureSummary` includes `projectId`/`identityKind`/`ownershipToken`; `subscribeRecent` filtered by `projectId`; `importClosures` gates ownership; no B->A leak via `pullDetail` | Owner change of schema3, schema3->schema2 |

Each slice is a <400-line work unit with its own tests; rollback is slice-exclusive.

## 10. Verification

- `npm test -- PayrollClosureB3Contract --runInBand` — contract audit (global queries, direct loadById, summary shape, wildcard, OFF, dormant LiveSync, stale guard, indexes).
- `npm test -- PayrollClosure --runInBand` — smoke: no regression on existing closure suites.
- `git diff --check` and `git diff --stat` must show only `docs/fase-1/F1.6-B3-contract.md` + `js/tests/PayrollClosureB3Contract.test.js` (<400 authored lines).
