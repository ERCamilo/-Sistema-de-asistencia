# F1.6-A0 — Bitácora: deduplicación IDB por proyecto (EntityProjectScope)

> **Estado vivo:** A0 está **✅ Cerrado y aprobado por Dirección** el **2026-08-26** (veredicto formal Dirección sobre `c7a9e0c` — `fix(projects): scope IDB entity deduplication by project` — y `333a516`; evidencia: RecordKey 13/13, F1.4+A0 12/12, acotado 76 tests, full suite 354/354 · 3409 · 0 fallos, fresh-context ALLOW). **A0.5 / DEP-SA-004 está 🟢 Autorizado / en ejecución (exclusivamente)** — `activeProjectId` local, `projectId` canónico account-level. Contrato congelado: `explicit projectId > current authoritative default > legacy-unresolved:*` (interno, nunca persistido, sin colisión destructiva); OFF paridad exacta. No habilita A1–A6 ni comportamiento económico de nómina. Esta bitácora es el registro evaluable de la unidad: qué se cambió, qué se descubrió, cómo se verificó y dónde se revisa.
>
> **Cierre 2026-08-26:** Dirección verificó tríada RED→GREEN (§2.1), contrato §2.2 y verificación §3; la cronología honesta se preserva intacta abajo. A0.5 es la única puerta autorizada.

| Campo | Valor |
|---|---|
| Unidad | F1.6-A0 — deduplicación de `IndexedDBService.saveState` por proyecto |
| Estado | ✅ Cerrado y aprobado por Dirección (veredicto 2026-08-26) · 🟢 A0.5 Autorizado / en ejecución |
| Fecha | 2026-08-26 (cierre formal A0) |
| Rama de validación | `fase-0-auditoria` (base donde se ejecutó la suite completa) |
| Rama actual | `feature/factor-dias-no-laborables` — contiene **12 modificados + 2 untracked WIP** de factor días no laborables que **no forman parte de A0/F1.6** y no deben mezclarse en la revisión |
| Commits A0 | `c7a9e0c` — fix(projects): scope IDB entity deduplication by project (F1.6 A0) · `333a516` — docs(fase-1): record A0 validation and project identity gate — **✅ Cerrado y aprobado por Dirección 2026-08-26** (RecordKey 13/13, F1.4+A0 12/12, 76 acotados, 354/354 · 3409 · 0 fallos, fresh-context ALLOW; contrato congelado: explicit projectId > default autoritativo > legacy-unresolved:*; OFF paridad exacta) |
| Suite de referencia | **354/354 suites · 3409 tests · 0 fallos · 189.372 s · sin reintentos** (`npm test -- --runInBand`) |
| Punto de parada | **A0 ✅ Cerrado y aprobado (2026-08-26)**; **A0.5 🟢 Autorizado / en ejecución exclusivamente**; A1–A6 y F1.7 permanecen bloqueadas hasta validar A0.5 |

## Resumen evaluable

Se corrigió la deduplicación global de `IndexedDBService.saveState` que colisionaba `#12 de A` contra `#12 de B` al persistir empleados y líderes. Con flag ON cada clave de dedup incluye proyecto efectivo (`project:<id>:num:42`); con flag OFF se conserva paridad byte-exacta `num:`/`id:` legacy; los registros legacy sin `projectId` ni default autoritativo quedan en namespace interno `legacy-unresolved:*` que nunca se persiste ni colisiona con namespaces explícitos. La unidad queda aislada en un módulo nuevo (`EntityProjectScope.js`) y 4 archivos integrados, con 2 suites enfocadas nuevas/bloqueadas (12/12 + 13/13) y validación acotada **7 suites · 76 tests**. Cualquier agente futuro puede evaluar A0 leyendo solo esta bitácora + el diff de `c7a9e0c`.

## Quick path (para el revisor)

1. Leer §2.2 Contrato final y §2.1 Tríada de fallos — entiende por qué el fix no es "agregar `projectId` a la clave".
2. Revisar §1 Cambios por archivo + frontera de rollback — verifica que el cambio es retirable sin tocar F1.5.
3. Ejecutar §3 Verificación — 2 comandos (suites enfocadas + full suite) reproducen la evidencia exacta de `c7a9e0c`.

Fuera de alcance de esta revisión: nómina (`PayrollService`, `PayrollPeriod`, `PayrollUI`, `payrollConfigsV1`), caja chica, y el WIP de factor días no laborables en la rama actual (ver §6).

## 1. Qué se hizo — cambios por archivo

| Archivo | Cambio | Rol en aislamiento |
|---|---|---|
| `js/modules/features/projects/EntityProjectScope.js` **(nuevo, 46 líneas)** | Módulo puro sin IO de negocio. Exporta `DEFAULT_PROJECT_LS_KEY`, `replaceEntityScope`, `peekEntityScope`, `captureEntityProjectScope`, `effectiveProjectId`, `entityInScope`, `sameEffectiveProject`. `EMPTY_SCOPE` frozen. `captureEntityProjectScope()` lee `FeatureFlags` + `localStorage` atómico por operación (`js/modules/features/projects/EntityProjectScope.js:25`). | Single source of truth del scope; evita duplicar lógica de proyecto efectivo en `RecordKey`/`IndexedDBService`/`ProjectContext`. |
| `js/modules/services/RecordKey.js` | `dedupKeyForRecord(record, scope = captureEntityProjectScope())` — si `!scope.enabled` retorna clave legacy exacta; si enabled resuelve `effectiveProjectId` y retorna `project:<id>:<recordKey>` o `legacy-unresolved:<recordKey>` (`js/modules/services/RecordKey.js:18`). Importa `captureEntityProjectScope`/`effectiveProjectId`. | Clave de dedup project-scoped determinista por operación; legacy-unresolved nunca se persiste como `projectId`. |
| `js/modules/services/IndexedDBService.js` | `saveState` captura `entityScope = captureEntityProjectScope()` **una vez** al inicio (`js/modules/services/IndexedDBService.js:894`) y pasa ese snapshot a ambos `dedupKeyForRecord` de empleados (`:930`) y líderes (`:943`). | Garantiza atomicidad OFF→ON / ON→OFF dentro del mismo grafo (ver §2.1 ciclo 2). |
| `js/modules/features/projects/DefaultProject.js` | Re-exporta `DEFAULT_PROJECT_LS_KEY` desde `EntityProjectScope` en lugar de definirlo local (`js/modules/features/projects/DefaultProject.js:27`). | Elimina duplicación de clave LS; el default autoritativo se lee solo vía `captureEntityProjectScope`. |
| `js/modules/features/projects/ProjectContext.js` | Migra `EMPTY_SCOPE`/`_resolvedScope` y helpers (`peekEntityScope`, `effectiveProjectId`, `entityInScope`, `sameEffectiveProject`) a `EntityProjectScope` vía `replaceEntityScope`/`peekEntityScope` (`js/modules/features/projects/ProjectContext.js:25`, `:96`, `:111`). `getEntityScope()` delega a `replaceEntityScope({enabled, projectId, defaultProjectId})` (`:145`). | Unifica estado mutable de scope; `setActiveProjectId`/`clearActiveProjectId` mantienen snapshot sincrónico coherente. |
| `js/tests/F14IsolationBattery.test.js` | +4 casos A0: `S2b` coexistencia A/B mismo `number` (`:207`), `S2c` OFF→ON same-graph con default mapping (`:241`), `S2d` ON→OFF paridad global legacy (`:280`), `S2e` legacy no resuelto vs explícito mismo número (`:305`). Total batería **12/12**. | Pruebas de integración IDB real (`fake-indexeddb`) que fijan los 3 ciclos de fallo. |
| `js/tests/IndexedDBSaveStateKeyTests.js` | +4 casos unitarios `RecordKey` (`:55`): OFF conserva `num:42`/`id:*` aunque exista `projectId`; ON incluye proyecto efectivo por `number` e `id`; legacy sin `projectId` resuelve a default no a activo; sin default usa `legacy-unresolved`. Total **13/13**. | Fija contrato de clave puro sin IO. |
| `js/modules/config/BuildInfo.js`, `sw.js` | Bump de build (cambio cosmético de versión). | Sin impacto funcional. |

**No tocados:** `PayrollService`, `PayrollPeriod`, `PayrollUI`, `payrollConfigsV1`, `PettyCash*`, `Attendance*` (salvo IDB dedup), `F1.7`.

## 2. Descubrimientos / Decisiones

### 2.1 Tríada de fallos — 3 ciclos RED→GREEN que sostienen el contrato

| Ciclo | RED observado | GREEN y causa raíz |
|---|---|---|
| **1 — Clave sin scope** | `RecordKey` esperaba `project:PRJ-A:num:42` y `project:PRJ-D:num:42` pero retornó `num:42`. IDB real: `employees:1`, `leaders:1`, `deduplicated:2` — registros de A desaparecieron (dedup global). | Extracción de `EntityProjectScope`; `dedupKeyForRecord` con scope; OFF paridad exacta; legacy→default. Suites iniciales: RecordKey **12/12**, F1.4 **9/9**, contexto **33/33**, IDB/tombstones **6/6**. |
| **2 — Scope stale en transición same-graph** | Revisión fresh-context detectó cache reutilizado al cambiar flag sin `getEntityScope()` intermedio. **OFF→ON:** `employees:2`, `leaders:1`, `deduplicated:4` — se perdían registros (scope deshabilitado cacheado). **ON→OFF:** `employees:2`, `leaders:2`, `deduplicated:0` — no se restauraba dedup global legacy. | Capturar `flag + default` **una vez por `saveState`** y propagar ese snapshot a todas las claves. GREEN: F1.4 **11/11**, RecordKey **12/12**, proyectos/default/context/boot **49/49**, IDB/tombstones **6/6**. Estadísticas: OFF→ON `employees:4`/`leaders:2`/`deduplicated:1` (solo colisión legacy `#13`); ON→OFF `employees:1`/`leaders:1`/`deduplicated:2` (paridad legacy restaurada). |
| **3 — Fallback obsoleto de default** | `captureEntityProjectScope()` usaba `readDefaultProjectId() ?? resolvedScope.defaultProjectId`. Si el puntero autoritativo actual está ausente, el fallback reutilizaba un `PRJ-OLD` cacheado obsoleto. Un legacy sin resolver se clasificaba bajo `PRJ-OLD` y se deduplicaba destructivamente contra un registro explícito `PRJ-OLD:num:12` (pérdida silenciosa). | Corrección final `c7a9e0c`: ausencia del puntero actual **no** promueve valor cacheado. Un legacy sin default autoritativo queda `legacy-unresolved`. Caso final GREEN **1/1** (`F14IsolationBattery S2e`); persistencia **13/13**. |

### 2.2 Contrato final de A0

- `record.projectId` explícito tiene prioridad.
- Si no existe, se usa el default autoritativo actual (`localStorage.getItem(DEFAULT_PROJECT_LS_KEY)` leído dentro de `captureEntityProjectScope()`).
- Si tampoco existe, el registro queda **legacy no resuelto**.
- Un legacy no resuelto usa clave interna `legacy-unresolved:<recordKey>` (p. ej. `legacy-unresolved:num:12`); **nunca** persiste un sentinel `projectId` ni se deduplica con un namespace explícito `project:*` — garantiza que `#12` legacy y `#12` explícito de `PRJ-OLD` coexistan (ver `F14IsolationBattery.test.js:305` — ambos sobreviven con `deduplicated:0`).
- Con flag OFF se conserva **paridad exacta** `num:`/`id:` legacy aunque los registros porten `projectId` (ver `IndexedDBSaveStateKeyTests.js:55`).
- `effectiveProjectId`/`sameEffectiveProject` mantienen hasOwnProperty: ausencia de `projectId` es ausencia, no `null` persistido.

### 2.3 Decisiones de arquitectura vigentes (no introducidas por A0, confirmadas por A0)

| Decisión | Contrato | Fuente |
|---|---|---|
| **DEP-SA-004** — identidad canónica `Project` cross-device como gate bloqueante | Misma cuenta/obra resuelve mismo `projectId` canónico en todos los dispositivos; `activeProjectId` sigue siendo selección local. Solución mínima SA-only (registro/adopción/promoción + guard contra carreras). Sin `organizationId`/roles/membresías/Mini/Integración. | `ROADMAP_BASE_SA_MINI_INTEGRACION.md:614` §12 · `docs/fase-1/F1.6-nomina-multiproyecto.md:110` §4.2 |
| **ADR-016** — `PayrollClosure` como autoridad de pago | Nueva `markAsPaid()` delega al cierre canónico o queda deshabilitada con proyectos ON; `employee.paymentHistory` se conserva como histórico, no como segundo ledger. | `ROADMAP_BASE_SA_MINI_INTEGRACION.md:639` §13 · `docs/fase-1/F1.6-nomina-multiproyecto.md:145` §4.5 |
| **Diferir `payrollConfigsV1/{projectId}` cloud** | Config cloud por `projectId` queda detrás de DEP-SA-004/A0.5. `ProjectStore` es solo IDB y `Project.makeProjectId()` genera IDs locales; sin identidad estable, cloud produciría configs paralelas o asociadas al proyecto equivocado. | `docs/fase-1/F1.6-nomina-multiproyecto.md:125` §4.3 · DEP-SA-002 |
| **H-05 redacción simétrica** | `exportConfig` es transitorio de UI/sesión; debe eliminarse de espejo `data/current`, replace cloud, snapshots, DataOps local→cloud e ingresos legacy cloud/snapshot. `settings.payrollDefaults` permanece durable hasta migración canónica. | `docs/fase-0/F0-HALLAZGOS.md:17` H-05 · `docs/fase-1/F1.6-nomina-multiproyecto.md:104` §4.1 |
| **PayrollProjectContext capturado** | Nómina consumirá adaptador con empleados/posiciones/líderes/asistencia/config ya scoped; `buildAttendanceIndex` permanece RAW intencionalmente. Snapshot antes de primer `await` para operaciones async. | `docs/fase-1/F1.6-nomina-multiproyecto.md:104` §4.1 · ADR-012/013/014 |

A0 no crea nuevos DEP/ADR; respeta IDs existentes y no duplica numeración.

## 3. Verificación

| Alcance | Comando | Resultado | Interpretación |
|---|---|---|---|
| RecordKey puro | `npm test -- js/tests/IndexedDBSaveStateKeyTests.js --runInBand` | **13/13** | Contrato de clave por `num:`/`id:` con/sin proyecto, OFF/ON, default y `legacy-unresolved` |
| Batería F1.4 + A0 | `npm test -- js/tests/F14IsolationBattery.test.js --runInBand` | **12/12** (4 nuevos S2b–S2e) | Coexistencia A/B, transiciones same-graph OFF→ON/ON→OFF, legacy no resuelto aislado |
| Suites F1.4 + contexto + IDB | `npm test -- js/tests/F14IsolationBattery.test.js js/tests/IndexedDBSaveStateKeyTests.js js/tests/ProjectContext.test.js js/tests/DefaultProject.test.js --runInBand` (agrupación representativa) | verdes (ver desglose acotado) | Scope, default y claves coherentes |
| **Validación acotada final** | `npm test -- --runInBand` filtrado a 7 suites A0/F1.4/proyectos/context/boot/IDB/tombstones | **7 suites · 76 tests · 0 fallos** | Work unit completo de A0 sin ruido de suite completa |
| **Suite completa post-A0** | `npm test -- --runInBand` | **354/354 suites · 3409 tests · 0 fallos · 189.372 s · sin reintentos** | Regresión completa verde; no sustituye revisión formal ni habilita F1.6 económica |
| Revisión fresh-context | Auditoría independiente del diff `c7a9e0c` | **ALLOW** | Confiabilidad técnica validada |
| Revisión RDD nativa | `gentle-ai review validate --gate pre-commit` | **invalidated** (RDD deshabilitado en repo) | No existe recibo nativo; aplica política ordinaria del repositorio |
| Runtime harness | `fake-indexeddb` (IDB real en Jest) | Sin browser harness | Evidencia runtime acotada a harness de pruebas, como en F1.0.1 |

Evidencia intermedia preservada en `F1.6-nomina-multiproyecto.md:203` §7 (ciclos 1–2 con estadísticas `deduplicated` y veredicto intermedio bloqueante) y §7.5 tabla final.

## 4. Checklist criterio de aceptación

- [x] Deduplicación IDB de empleados y líderes por proyecto con flag ON (`project:<id>:num:` / `project:<id>:id:`) y paridad exacta `num:`/`id:` con flag OFF
- [x] Registros legacy sin `projectId` resuelven a default autoritativo actual, no a `activeProjectId`; sin default quedan `legacy-unresolved:*` sin colisionar con namespaces explícitos
- [x] Transición same-graph OFF→ON no pierde registros y ON→OFF restaura dedup global legacy (scope capturado por operación)
- [x] `effectiveProjectId`/`sameEffectiveProject` preservan hasOwnProperty: `projectId` ausente nunca se persiste como sentinel
- [x] Módulo nuevo `EntityProjectScope.js` con `captureEntityProjectScope` y re-export de `DEFAULT_PROJECT_LS_KEY`; integración en `RecordKey`/`IndexedDBService`/`ProjectContext`/`DefaultProject`
- [x] Tests RED→GREEN: `IndexedDBSaveStateKeyTests` **13/13** + `F14IsolationBattery` **12/12** (S2b–S2e)
- [x] Validación acotada **7 suites · 76 tests · 0 fallos** + suite completa **354/354 · 3409 · 0 fallos** (189.372 s)
- [x] Revisión fresh-context **ALLOW**; RDD nativo `invalidated` documentado como no bloqueante
- [x] Frontera de rollback explícita y sin arrastre a F1.5 ni a WIP de factor días no laborables
- [x] Revisión formal de Dirección — **✅ Cerrado y aprobado por Dirección (2026-08-26)** sobre `c7a9e0c` + esta bitácora (habilita A0.5)
- [ ] A0.5 / DEP-SA-004 — **🟢 Autorizado / en ejecución** (identidad canónica `Project` cross-device — `activeProjectId` local, `projectId` canónico account-level; en curso, no validado aún)

## 5. Riesgos / Bloqueos y Próximo paso (A0.5)

| ID / Tema | Estado | Impacto | Resolución requerida |
|---|---|---|---|
| A0 dedup IDB | **✅ Cerrado y aprobado por Dirección (2026-08-26)** — veredicto formal sobre `c7a9e0c`; contrato congelado y evidencia §3 preservada | Habilita A0.5; no habilita por sí sola A1–A6 ni F1.6 económica | Verificado 2026-08-26 (RecordKey 13/13, F1.4+A0 12/12, 76 acotados, 354/354 · 3409, fresh-context ALLOW) |
| A0.5 / DEP-SA-004 | **🟢 Autorizado / en ejecución — bloqueante para A1–A6** | Bloquea A1–A6, estado económico durable por `projectId` y cloud F1.6-B hasta validar identidad canónica | Registro/adopción/promoción mínima SA-only + guard contra carreras cross-tab; validación cross-device (ver `F1.6-nomina-multiproyecto.md:110` §4.2); `activeProjectId` local, `projectId` canónico account-level |
| Identidad cross-device | No resuelta | Dos dispositivos pueden asignar IDs distintos al mismo proyecto predeterminado | Resolver A0.5 antes de `payrollConfigsV1/{projectId}` cloud y economía durable |
| `markAsPaid` / ADR-016 | Decisión fijada; implementación pendiente | Segunda bitácora `paymentHistory` puede divergir del cierre canónico | Delegar a `PayrollClosure` o deshabilitar con proyectos ON en B4 |
| H-05 `exportConfig` | Veredicto resuelto; código pendiente | Estado transitorio puede reaparecer por mirror/snapshot/replace | Sanitización simétrica A5 |
| WIP factor días no laborables | 12 modificados + 2 untracked en `feature/factor-dias-no-laborables` | Riesgo de mezclar trabajo no relacionado en la revisión de A0 | Mantener ramas/worktrees separados; no incluir en diff de A0 |

### Próximo paso seguro (orden de Dirección — actualizado 2026-08-26)

1. **A0 ✅ Cerrado y aprobado por Dirección el 2026-08-26** — veredicto formal sobre `c7a9e0c` + esta bitácora y `F1.6-nomina-multiproyecto.md:243` §7.5; cronología RED→GREEN preservada.
2. **Ejecutar A0.5 / DEP-SA-004 — 🟢 Autorizado / en ejecución exclusivamente** (identidad canónica `Project` cross-device; `activeProjectId` local, `projectId` canónico account-level); no existe código A0.5 previo en este estado.
3. Mantener **A1–A6 y F1.7 bloqueadas** hasta validar A0.5; no implementar todavía configuración cloud ni comportamiento económico de F1.6-B.

### Frontera de rollback

Retirar **exclusivamente**:

- `js/modules/features/projects/EntityProjectScope.js`;
- integración de scope en `js/modules/features/projects/ProjectContext.js`, `js/modules/features/projects/DefaultProject.js`, `js/modules/services/RecordKey.js`, `js/modules/services/IndexedDBService.js`;
- casos A0 en `js/tests/IndexedDBSaveStateKeyTests.js` y `js/tests/F14IsolationBattery.test.js` (S2b–S2e + asserts de clave).

Corresponde al commit `c7a9e0c`; no incluye A0.5, no incluye comportamiento de nómina y no autoriza revertir el índice documental vivo de F1.5 ni trabajo paralelo ajeno.

## 6. Referencias

| Tipo | Referencia |
|---|---|
| Commits | `c7a9e0c` fix(projects): scope IDB entity deduplication by project (F1.6 A0) · `333a516` docs(fase-1): record A0 validation and project identity gate |
| Archivos — implementación | `js/modules/features/projects/EntityProjectScope.js:1` · `js/modules/services/RecordKey.js:15` · `js/modules/services/IndexedDBService.js:894` · `js/modules/features/projects/ProjectContext.js:25` · `js/modules/features/projects/DefaultProject.js:27` |
| Archivos — tests | `js/tests/IndexedDBSaveStateKeyTests.js:55` · `js/tests/F14IsolationBattery.test.js:207` (S2b), `:241` (S2c), `:280` (S2d), `:305` (S2e) |
| Docs fase | [`F1.6-nomina-multiproyecto.md`](F1.6-nomina-multiproyecto.md) §7 (cronología honesta A0, 344 líneas) · [`../fase-0/F0-HALLAZGOS.md`](../fase-0/F0-HALLAZGOS.md) H-05 · [`README.md`](README.md) índice F1 (esta bitácora enlazada en Pasos) · [`F1.0.1-idb-real.md`](F1.0.1-idb-real.md) harness IDB real |
| Roadmap | `ROADMAP_BASE_SA_MINI_INTEGRACION.md:606` §12 DEP-SA-002/003/004 · §13 ADR-012 a ADR-016 · §7 Fase 1 (F1.6 bloqueada hasta A0.5) |
| Orden de Dirección | F1.6: A0 → A0.5 → A1 → A2 → A3 → A4 → A5 → A6 (ver `F1.6-nomina-multiproyecto.md:6` y `README.md:6`) |

### Nota sobre rama actual

La rama `feature/factor-dias-no-laborables` contiene trabajo en curso ajeno a A0. `git status --short` al 2026-08-26 reporta 12 modificados (`css/attendance_ui.css`, `js/app.js`, `js/boot-loader.js`, `js/modules/features/analytics/*`, `js/modules/features/employees/*`, `js/modules/features/payroll/PayrollService.js`, `js/modules/features/profile/*`, `js/modules/ui/*`, `sw.js`) + 2 untracked (`js/modules/features/payroll/PayrollFactors.js`, `js/tests/RestDayFactorResolutionTests.test.js`). **Ninguno de esos archivos forma parte del diff `c7a9e0c..333a516` ni de esta bitácora.** La revisión de A0 debe basarse exclusivamente en `c7a9e0c`/`333a516` y en la suite `354/354 · 3409`.

## 7. Convención documental preservada

Esta bitácora replica el sistema `docs/fase-0` / `docs/fase-1`: título `F1.6-A0`, tabla `| Campo | Valor |`, blockquote **Estado vivo**, secciones `Qué se hizo` / `Descubrimientos` / `Verificación` / `Checklist` / `Próximo paso`, y enlace en `docs/fase-1/README.md` Pasos. Mantiene tono español neutro/profesional, denso pero escaneable (tablas + checklists + progressive disclosure), y no introduce nuevos IDs DEP/ADR.
