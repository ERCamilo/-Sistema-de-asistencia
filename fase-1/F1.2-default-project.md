# F1.2 — Proyecto Predeterminado idempotente

| Campo | Valor |
|---|---|
| Paso | Fase 1 / F1.2 — hito `default-project` |
| Fecha | 2026-08-25 |
| Estado | ✅ Ejecutado + segunda vuelta (2 advertencias LATENTES registradas) |
| Criterio roadmap | Usuario existente entra sin configuración manual — **parcial**: se cumple al conectar el boot-wiring (deferido a propósito, ver abajo) |

## Qué se construyó

`js/modules/features/projects/DefaultProject.js` (82 líneas):

1. `ensureDefaultProject()` idempotente:
   - Guard interno: flag OFF ⇒ devuelve null SIN tocar LS/IDB (probado por test).
   - Puntero persistente en LS `asistencia_default_project_id` (elección: preferencia device-local sobre datos local-first; lectura O(1) en boot; sin migración de esquema).
   - Pointer-miss/puntero-colgado ⇒ recuperación determinista: primer proyecto ACTIVO por `createdAt` ascendente (listAll ya ordena así) o creación única `"Mi obra"` con re-pointeo.
2. Crash entre create y writePointer se autorepara con el escaneo (solo existe un candidato).

## Boot-wiring DEFERIDO (decisión declarada)

El sitio correcto fue **verificado independientemente como exacto**: `initializeApp()` en `app.js` (~línea 7047, justo tras `hydrateApplicationAndInitializeWeather`, dentro del try, con Modal/showNotification disponibles). NO se conectó porque había trabajo paralelo del dueño en el árbol y el flag está inactivo — prioridad cero-riesgo. Quedó `TODO(boot-wiring F1.2)` con snippet recomendado dentro del módulo.

## Segunda vuelta — hallazgos LATENTES (inactivos hoy: flag OFF + sin wiring)

| ID | Sev. | Hallazgo | Cuándo muerde |
|---|---|---|---|
| **W1** | 🟡 | Pointer-HIT no valida status: si el default se cerrara/archivara (posible desde F1.4), `getActiveProjectId()` entregaría un id no-activo — diverge del recovery-path que sí filtra activos | Al habilitar flag+wiring con ciclos de cierre |
| **W2** | 🟡 | Carrera cross-tab: dos pestañas con puntero ausente crearían dos "Mi obra"; el perdedor queda duplicado permanente (CrossTabLock existe en el repo pero no se usa aquí) | Primera sesión multip-pestaña con flag ON |

**Disposición recomendada:** corregir W1+W2 como PRIMER acto del próximo slice autorizado, ANTES de conectar el boot-wiring o encender el flag. Ninguno afecta el punto de parada actual.

## Aceptación

- [x] Idempotencia probada (N llamadas = un solo default)
- [x] Recuperación ante puntero perdido/colgado, determinista
- [x] Flag OFF ⇒ nada se crea
- [x] Tests IDB real: 6/6
- [ ] Criterio "entra sin configuración manual" end-to-end — requiere wiring (bloqueado a propósito)
